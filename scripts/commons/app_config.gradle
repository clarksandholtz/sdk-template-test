/**
 * App-only configuration
 * */
apply plugin: "com.android.application"

// TODO update sample package name according to your project
android.defaultConfig.applicationId = "com.clarksandholtz.sdktest.sample"

apply from: rootProject.file("scripts/commons/common_config.gradle")